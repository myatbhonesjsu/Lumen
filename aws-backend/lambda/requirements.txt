boto3==1.34.19
requests==2.31.0

# Optional: For vector search (adds ~500MB, may require Lambda layer)
# Uncomment if you want semantic search with embeddings
# sentence-transformers==2.2.2
# torch==2.0.1
# numpy==1.24.3

# Note: Without these, vector_search.py will fallback to keyword search
# which is still effective for our use case
